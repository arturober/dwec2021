import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.arturober.ionicproducts',
  appName: 'Ionic products',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
