import { SetBgcolorDirective } from './set-bgcolor.directive';

describe('SetBgcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new SetBgcolorDirective();
    expect(directive).toBeTruthy();
  });
});
